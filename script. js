form.addEventListener('submit',function(e)) {
    e.preventdefault();
    console.log({
        username:username.valiu,
        fullName:fullName.valiu,
        email:email.valiu,
        password:"*******"

    });
 alert("successfull");
 form.reset();
 submitbtn.disabled = true;
};

